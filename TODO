
- Expire older data, when new PUT appears, and old data exists to be expired.

- Implement total storage byte limit.  PUTs refused when limit hit
  (and nothing to expire).

- Use chunked upload rather than all-in-memory approach.

- delete, iff signed requested matches previously stored public key

- pay to extend expiration date

- variable pricing

- CLI client

