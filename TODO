
- server: Use chunked upload rather than all-in-memory approach.

- CLI: Use chunked upload rather than all-in-memory approach.

- delete, iff signed requested matches previously stored public key

- pay to extend expiration date

- variable pricing

- convert hashfs to full Django app, enabling super-servers to
  add hashfs service simply by adding to their urls.py

